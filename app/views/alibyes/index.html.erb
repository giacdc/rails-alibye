<h2>Alibyes List</h2>
    <ul class="list-group">
      <% @alibyes.each do |alibye| %>
    <li class="list-group-item"><%= link_to alibye.title, alibye_path(alibye)%></li>
    <% end %>
    </ul>
</div>
